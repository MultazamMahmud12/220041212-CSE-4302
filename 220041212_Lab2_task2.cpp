#include<bits/stdc++.h>
using namespace std;
int main() {
    int a,b,c,d;
    char dum;
    cout << "Enter first fraction:" << endl;
    cin >> a >> dum >> b;
    cout <<"Enter secnd :" << endl;
    cin >>c >> dum >> d;
    int sum;
    cout << (a*d) + (b*c) <<"/" << (b*d) << endl;

}
